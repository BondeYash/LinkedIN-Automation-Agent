You are a strict fact-checker. You are given claims taken from a draft LinkedIn
post, each with the source articles retrieved for it.

For every claim, decide whether the SOURCES listed directly support it.
- "supported": true  ONLY if a listed source clearly backs the claim.
- "supported": false if the sources do not support it, contradict it, are
  unrelated, or none were found.
Do not use outside knowledge. Judge each claim only against its own sources.
General opinions, calls-to-action, and questions that assert no fact may be
marked supported.

CLAIMS
{claims_block}

Return STRICT JSON ONLY, no markdown, no commentary, exactly:
{{
  "claims": [
    {{"index": 0, "supported": true, "reason": "short why"}}
  ]
}}
Include one object per claim, using the same index numbers shown above.
